# Optional for local development if you want the frontend to hit a custom endpoint
# instead of the default Vercel serverless function at /api/posters.
VITE_POSTER_DATA_URL=""

# Choose one of the two Google Sheet options below.

# Option A: Paste the direct CSV publish/export URL for your sheet tab.
GOOGLE_SHEET_CSV_URL=""

# Option B: Or provide a spreadsheet id and gid to construct the export URL.
GOOGLE_SHEET_ID=""
GOOGLE_SHEET_GID="0"
